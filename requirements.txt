# ── 핵심 ML ──────────────────────────────────────────────
torch>=2.0.0
transformers>=4.35.0

# ── 오디오 처리 ───────────────────────────────────────────
librosa>=0.10.0
sounddevice>=0.4.6
scipy>=1.11.0
numpy>=1.24.0

# ── 발음 평가 ─────────────────────────────────────────────
jiwer>=3.0.0

# ── G2P (발음 전사) ───────────────────────────────────────
# g2pk/g2pk2/g2pkk → Windows MeCab/eunjeon 빌드 오류 발생
# kss → pecab 백엔드, MeCab 불필요, Windows/macOS/Linux 모두 동작
kss>=6.0.0

# ── TTS ──────────────────────────────────────────────────
gtts>=2.3.0
pyttsx3>=2.90

# ── 노이즈 제거 (선택, 설치 시 자동 적용) ────────────────
# noisereduce>=2.0.0
